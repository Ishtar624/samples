% Copyright

class baseForm : baseForm
    open core

predicates
    display : (window Parent) -> baseForm Form.

constructors
    new : (window Parent).

properties
    filename : string.

end class baseForm
